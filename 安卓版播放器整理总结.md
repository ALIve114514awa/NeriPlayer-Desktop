# NeriPlayer Android 播放器架构整理总结

> 整理时间：2026-04-26
> 基于 NeriPlayer-Android 源码分析

---

## 一、核心架构模式

单例 `PlayerManager`（Kotlin `object`）持有 ExoPlayer 实例和所有播放状态。UI 层（Jetpack Compose）通过 `StateFlow.collectAsState()` 直接订阅。逻辑按职责拆分到多个扩展文件。

```
┌─ UI Layer (Compose) ─────────────────────────────┐
│  NowPlayingScreen / LyricsScreen / MiniPlayer     │
│  ↕ collectAsState()                               │
├─ State Layer ─────────────────────────────────────┤
│  PlayerManager (singleton, ~15 StateFlow)          │
│  ↕                                                │
├─ Engine Layer ────────────────────────────────────┤
│  ExoPlayer + SimpleCache + PlaybackEffectsController│
│  ↕                                                │
├─ Service Layer ───────────────────────────────────┤
│  AudioPlayerService (MediaSessionCompat + 通知栏)  │
│  ↕                                                │
├─ Source Layer ────────────────────────────────────┤
│  Netease / Bilibili / YouTube Music / Local File   │
└───────────────────────────────────────────────────┘
```

---

## 二、文件清单与职责

### 2.1 播放核心

| 文件 | 路径 | 职责 |
|------|------|------|
| PlayerManager.kt | `core/player/` | 单例核心，持有 ExoPlayer、所有 StateFlow、公开 API |
| PlayerManagerLifecycleExtensions.kt | `core/player/` | `initializeImpl()` / `releaseImpl()` — ExoPlayer 创建、Cache 初始化、Listener 注册、settings 订阅、状态恢复 |
| PlayerManagerPlaybackExtensions.kt | `core/player/` | 所有传输控制实现：playAtIndex、pause、seek、next、prev、repeat、进度更新循环 |
| PlayerManagerUrlExtensions.kt | `core/player/` | `resolveSongUrl()` — 多平台 URL 解析调度 |
| PlayerManagerPersistenceExtensions.kt | `core/player/` | 队列和状态持久化、收藏、歌词、元数据更新 |
| PlayerManagerYouTubePrefetchExtensions.kt | `core/player/` | YouTube 即将播放曲目的 URL 预热 |

### 2.2 服务与音效

| 文件 | 路径 | 职责 |
|------|------|------|
| AudioPlayerService.kt | `core/player/` | 前台 Service、MediaSessionCompat、通知栏控件、becoming noisy 处理 |
| PlaybackEffectsController.kt | `core/player/` | Equalizer + LoudnessEnhancer + PlaybackParameters（速度/音调） |
| SleepTimerManager.kt | `core/player/` | 睡眠定时器（倒计时/播完当前/播完列表） |

### 2.3 网络与数据源

| 文件 | 路径 | 职责 |
|------|------|------|
| ConditionalHttpDataSourceFactory.kt | `core/player/` | 按平台注入请求头（Bili Referer、YouTube Auth、Cookie） |
| PlayerUrlResolver.kt | `core/player/url/` | 各平台音质标签、MIME 标准化、码率估算 |
| NeteasePlaybackResponseParser.kt | `core/player/` | 解析网易云下载/播放 API 响应，检测试听片段 |
| YouTubeGoogleVideoRangeSupport.kt | `core/player/` | Google Video CDN range 请求头处理 |
| YouTubeSeekRefreshPolicy.kt | `core/player/` | 判断 YouTube URL 在 seek 前是否需要刷新 |

### 2.4 策略与辅助

| 文件 | 路径 | 职责 |
|------|------|------|
| PlayerPlaybackPolicy.kt | `core/player/policy/` | 纯函数：shouldShowPauseButton、shouldPauseWhenToggling、resolvePlaybackStartPlan 等 |
| PlayerManagerPlaybackFailurePolicy.kt | `core/player/policy/` | 播放失败后的 NEXT/WRAP/STOP 决策 |
| TrackEndDeduplication.kt | `core/player/` | 防止 ExoPlayer 对同一曲目重复触发结束事件 |
| MediaSessionPlaybackStateThrottler.kt | `core/player/` | 限制 MediaSession 状态更新频率（最小 1s 间隔） |
| ReactiveRenderersFactory.kt | `core/player/` | 自定义渲染器工厂（FFmpeg 解码 + 音频可视化数据） |
| AudioReactive.kt | `core/player/` | 实时音频电平/节拍数据，用于背景动画 |

### 2.5 数据模型

| 文件 | 路径 | 职责 |
|------|------|------|
| PersistedPlayerState.kt | `core/player/model/` | 持久化用的数据类（PersistedSongItem、PersistedPlaybackState） |
| SongUrlResult.kt | `core/player/model/` | Sealed class: Success / WaitingForAuthoritativeStream / RequiresLogin / Failure |
| PlaybackAudioInfo.kt | `core/player/model/` | 音质元数据模型：来源、音质标签、编解码器、码率、采样率 |
| PlaybackSoundModels.kt | `core/player/model/` | 速度/音调/EQ 配置和状态模型 |
| PlayerEvent.kt | `core/player/model/` | Sealed class: ShowError / ShowLoginPrompt |

### 2.6 UI 组件

| 文件 | 路径 | 职责 |
|------|------|------|
| NowPlayingScreen.kt (~3400行) | `ui/screen/` | 全屏播放页 UI（封面、控件、进度条、队列、各种 BottomSheet） |
| LyricsScreen.kt (~1122行) | `ui/screen/` | 全屏歌词页（AdvancedLyricsView 或 AppleMusicLyric） |
| NeriMiniPlayer.kt | `ui/component/` | 底部迷你播放条（64dp，封面+标题+播放按钮） |
| WaveformSlider.kt | `ui/component/` | 自定义 Canvas 正弦波进度条 |
| AppleMusicLyric.kt | `ui/component/` | 逐行歌词组件（LazyColumn + scale/blur/auto-scroll） |
| AdvancedLyricsView.kt | `ui/component/` | 逐字歌词组件（封装 accompanist-lyrics-ui） |
| NowPlayingViewModel.kt | `ui/viewmodel/` | 手动搜索匹配、下载管理、音效设置、歌词填充 |

---

## 三、PlayerManager 关键状态变量

```kotlin
// 播放器实例
player: ExoPlayer
cache: SimpleCache  // LRU 1GB 流媒体缓存

// 播放队列
currentPlaylist: List<SongItem>
currentIndex: Int

// 响应式状态 (StateFlow)
_currentSongFlow          // 当前歌曲
_isPlayingFlow            // ExoPlayer 实际播放状态
_playbackControlPlayingFlow // 用户意图播放状态（UI 用）
_playWhenReadyFlow
_playerPlaybackStateFlow  // IDLE / BUFFERING / READY / ENDED
_playbackPositionMs       // 当前播放位置（每 80ms 更新）
_shuffleModeFlow
_repeatModeFlow
_currentMediaUrl          // 当前解析到的流 URL
_currentPlaybackAudioInfo // 当前音质信息

// 控制变量
playbackRequestToken      // 单调递增，取消过期异步任务
resumePlaybackRequested   // 用户意图标志，解耦 UI 图标
pendingSeekPositionMs     // seek 目标位置（防进度条回弹）
consecutivePlayFailures   // 连续失败计数，上限 10

// Shuffle 三栈
shuffleBag      // 未播放索引池
shuffleHistory  // 已播放索引栈（供 previous 使用）
shuffleFuture   // 队列中即将播放的索引（供 next 使用）
```

---

## 四、播放控制流程

### 4.1 播放一首歌 (`playAtIndex`)

```
1. 取消旧的 playJob，设置 _currentSongFlow
2. resumePlaybackRequested = true     ← UI 立即显示暂停图标
3. 检查 Listen Together 状态
4. 在 IO 线程启动 playJob:
   a. resolveSongUrl(song) → 多平台调度
   b. 成功:
      - 构建 MediaItem（自定义 cache key）
      - 可选 crossfade（交叉淡入淡出）
      - setMediaItem → prepare → play + fadeIn
   c. 失败:
      - consecutivePlayFailures++
      - advanceAfterPlaybackFailure()
   d. RequiresLogin:
      - 弹登录提示
      - 自动 skip next
5. 预热下一首 YouTube URL
```

### 4.2 暂停 (`pauseImpl`)

```
if (fadeOut enabled) {
    launch pendingPauseJob → 渐降音量 → pause
} else {
    immediate pause
}
persist state: shouldResumePlayback = false
```

### 4.3 继续播放 (`playImpl`)

```
if (player prepared && URL 未过期 < 10min) → 直接 resume + fadeIn
elif (URL 过期 || YouTube 需刷新) → refreshCurrentSongUrl()
elif (player 未 prepared && 队列存在) → resolveManualResumePlaybackDecision()
```

### 4.4 Seek (`seekToImpl`)

```
1. YouTube: 检查 SeekRefreshPolicy（部分 URL 是 one-shot）
2. 设置 pendingSeekPositionMs（防止进度条回弹）
3. player.seekTo(positionMs)
```

### 4.5 下一曲 (`nextImpl`)

```
Shuffle ON:
  1. shuffleFuture 有值 → pop
  2. shuffleBag 有值 → random pick
  3. bag 空 → rebuildShuffleBag (if repeat_all or force)

Shuffle OFF:
  index++ → 末尾时 wrap (if repeat_all or force)

支持 crossfade 切歌
```

### 4.6 上一曲 (`previousImpl`)

```
Shuffle ON:  pop shuffleHistory → push current to shuffleFuture
Shuffle OFF: index-- → wrap if repeat_all
```

### 4.7 循环模式 (`cycleRepeatModeImpl`)

```
OFF → ALL → ONE → OFF
```

### 4.8 曲目结束 (`handleTrackEnded`)

```
1. 检查 SleepTimer
2. REPEAT_ONE → replay current
3. REPEAT_ALL → next(force=true)
4. REPEAT_OFF + sequential → next if not last, else stop
5. REPEAT_OFF + shuffle → next if bag/future not empty, else stop
```

---

## 五、进度追踪机制

| 项目 | 值 |
|------|-----|
| 更新频率 | 80ms (`PLAYBACK_PROGRESS_UPDATE_INTERVAL_MS`) |
| 进度来源 | `player.currentPosition` |
| Seek 防回弹 | `pendingSeekPositionMs` — 在 ExoPlayer 实际跳转到位前（1.5s 容差），UI 显示用户拖拽目标位置 |
| 状态持久化 | 播放中每 15s 写一次 position |
| 持久化防抖 | `scheduleStatePersist()` 250ms debounce |

Desktop 端对比：使用 `requestAnimationFrame` 驱动的 `interpolatedPositionMs`，原理类似但帧率更高。

---

## 六、音频源解析 (`resolveSongUrl`)

### 6.1 解析优先级

| 优先级 | 来源 | 解析方式 |
|-------|------|---------|
| 1 | Listen Together 等待权威流 | 返回等待状态 |
| 2 | 直接 HTTP URL (`song.streamUrl`) | 直接使用 |
| 3 | 本地文件 | `file://` URI，检查文件可读性 |
| 4 | 本地下载缓存 | `AudioDownloadManager.getLocalPlaybackUri()` |
| 5 | ExoPlayer 流缓存（完整） | `http://offline.cache/{key}` |
| 6 | 平台 API 解析（见下表） | 按平台调度 |
| 7 | ExoPlayer 缓存 fallback（部分数据） | 兜底 |

### 6.2 各平台解析

| 平台 | 识别方式 | 解析函数 | Cache Key |
|------|---------|---------|-----------|
| Netease | 默认（非 YouTube/Bili） | `getNeteaseSongUrl()` — 音质逐级降级 | `netease-{songId}-{quality}` |
| Bilibili | `album.startsWith("Bilibili")` 或 `channelId` | `resolveBiliSong()` → `getAudioWithDecision()` | `bili-{songId}-{cid}-{quality}` |
| YouTube Music | `isYouTubeMusicSong()` 或 `channelId` | `getBestPlayableAudio(videoId)`，偏好 Opus | `ytmusic-{videoId}-{quality}-m4a` |
| Local | `LocalSongSupport.isLocalSong()` | `file://` URI | `local-{stableKey.hashCode()}` |

### 6.3 Netease 音质降级顺序

请求时按以下顺序尝试，直到成功：
```
jymaster → sky → jyeffect → hires → lossless → exhigh → higher → standard
```

---

## 七、队列管理

队列实现为简单的 `List<SongItem>` + 手动索引跟踪，**不使用 ExoPlayer 内置 shuffle**。

### Shuffle 三栈模型

```
shuffleBag:     未播放索引池（当前周期剩余可选曲目）
shuffleHistory: 已播放索引栈（支持 previous 回溯）
shuffleFuture:  预排队索引（来自"下一首播放"或 previous 回退）
```

### 队列操作

- `addToQueueNext()`: 插入到当前曲目后面，去重，更新 shuffle bag
- `addToQueueEnd()`: 追加到末尾，去重，重建 shuffle bag
- `playFromQueue()`: 跳转到指定索引，更新 shuffle history

---

## 八、状态持久化

### 8.1 文件结构

| 文件 | 内容 | 写入时机 |
|------|------|---------|
| `last_playlist.json` | 完整队列 + 嵌入数据 | 队列引用变化时（identity check） |
| `last_playback_state.json` | 索引、位置、resume 标志、模式 | 每次有意义的状态变化 |

### 8.2 持久化策略

- 写入防抖: `scheduleStatePersist()` — 250ms debounce
- 进度持久化节流: `maybePersistPlaybackProgress()` — 15s 间隔
- Mutex 保护写入，Gson 序列化
- 仅当前歌曲保存歌词数据（节省空间）

### 8.3 状态恢复

```
App 启动 → loadRestoredStateSnapshot() [IO 线程]
         → 过滤不可读本地文件
         → 验证索引有效性
         → applyRestoredStateSnapshot() 设置所有状态变量
         → 等待 Service 就绪
         → resumeRestoredPlaybackIfNeeded() 恢复播放
```

---

## 九、UI 交互细节

### 9.1 NowPlaying 打开/关闭

- **不是路由**，是 `AnimatedVisibility` 覆盖层，由 `showNowPlaying` 状态控制
- 打开动画: `slideInVertically(300ms, FastOutSlowInEasing)` 从底部滑入 + `fadeIn(150ms)`
- 关闭动画: `slideOutVertically(250ms)` + `fadeOut(150ms)`
- 关闭触发: 下拉手势 (>60px) 或返回键
- 始终强制 dark theme，动态色从封面提取

### 9.2 WaveformSlider (正弦波进度条)

```
Canvas 绘制: sin(x * 0.08 + phase)
振幅: 6px (播放时)，0px (暂停时，500ms 过渡)
波浪动画: infiniteRepeatable, 0→2PI, 2000ms 周期

未播放部分: onSurface.copy(alpha=0.3f), stroke 4f
已播放部分: primary color, stroke 6f, clipRect
Thumb: 圆形 radius=16f, 跟随波浪 Y 坐标

拖拽: detectDragGestures → position.x / width → clamp [0,1]
松手: onValueChangeFinished → 触发 seek
```

### 9.3 播放/暂停按钮

```
观察: playbackControlPlayingFlow（用户意图）
  而非 isPlayingFlow（ExoPlayer 实际状态）
  → 避免 fadeIn/fadeOut 期间的图标延迟

图标切换: AnimatedContent + scaleIn/scaleOut + fadeIn/fadeOut
点击: PlayerManager.togglePlayPause()
```

### 9.4 MiniPlayer

```
高度: 64dp
封面: 40dp, Coil AsyncImage (128px 缩略图)
毛玻璃: Haze 库 (API 31+), container alpha 0.4
圆角: 仅顶部 20dp
点击: 展开 NowPlayingScreen
```

### 9.5 歌词页面切换

```
主页面 ↔ 歌词: AnimatedContent + fadeIn/fadeOut(300ms)
进入歌词: 左滑 (<-20px)
返回主页: 右滑 (>50px) 或返回键
所有控件: sharedElement key 实现丝滑过渡动画
```

### 9.6 歌词渲染模式

| 模式 | 组件 | 启用条件 |
|------|------|---------|
| 逐字高亮 | `AdvancedLyricsView` (封装 accompanist-lyrics-ui) | `advancedLyricsEnabled = true`（默认） |
| 逐行高亮 | `AppleMusicLyric` | `advancedLyricsEnabled = false` |

### 9.7 封面显示规格

| 位置 | 尺寸 | 圆角 | 加载策略 |
|------|------|------|---------|
| NowPlaying 主页 | `maxWidth * 0.6` 或 `maxHeight * 0.65` | 24dp | offlineCachedImageRequest, allowHardware=false |
| LyricsScreen 顶栏 | 64dp * coverScale | - | sharedElement 过渡 |
| MiniPlayer | 40dp | 8dp | fastScrollableImageRequest(128px) |

---

## 十、关键设计模式

| 模式 | 说明 | 解决的问题 |
|------|------|-----------|
| **resumePlaybackRequested** | 用户意图与播放器实际状态解耦 | 防止 UI 图标在 fadeIn/fadeOut 期间抖动 |
| **playbackRequestToken** | 单调递增 token | 取消过期的异步 URL 解析任务 |
| **pendingSeekPositionMs** | seek 时 UI 显示目标位置 | 避免进度条在 ExoPlayer seek 完成前回弹 |
| **consecutivePlayFailures + 熔断** | 连续失败 10 次后停止 | 防止无限 skip |
| **scheduleStatePersist (250ms debounce)** | 状态写盘防抖 | 减少磁盘 IO |
| **maybePersistPlaybackProgress (15s throttle)** | 进度持久化节流 | 平衡恢复精度与 IO 开销 |
| **手动 Shuffle (Fisher-Yates)** | 自维护 bag/history/future 三栈 | 支持 previous 回溯、queue next 插入 |
| **crossfade** | 切歌时交叉淡入淡出 | 平滑的听感过渡 |
| **URL 过期检测 (10min)** | 流 URL 有时效性 | 长时间暂停后恢复播放不出错 |

---

## 十一、AudioPlayerService 详解

### 11.1 Service 类型

- 前台 Service，`START_STICKY`（播放中）/ `START_NOT_STICKY`（暂停时）
- 使用 legacy `MediaSessionCompat`（非 Media3 MediaSession）

### 11.2 通知栏

```
MediaStyle 通知:
  按钮: [Previous] [Play/Pause] [Favorite] [Next]
  紧凑视图: [Previous] [Play/Pause] [Next]
  封面: Coil 异步加载，设为 METADATA_KEY_ALBUM_ART
  副标题: 睡眠定时器状态（激活时显示）
```

### 11.3 MediaSession Actions

```
PLAY, PAUSE, PLAY_PAUSE, SKIP_NEXT, SKIP_PREVIOUS, SEEK_TO
自定义: ACTION_TOGGLE_FAV（切换收藏）
注意: 不包含 STOP（STOP 命令被降级为 pause）
```

### 11.4 状态更新节流

`MediaSessionPlaybackStateThrottler` 限制 `setPlaybackState()` 调用频率：
- 播放中: 最小 1s 间隔
- seek 漂移检测: 超过阈值时立即更新

### 11.5 特殊处理

- `becomingNoisyReceiver`: 耳机拔出时自动暂停
- `onTaskRemoved()`: 从最近任务划掉时抑制自动恢复（不杀播放）
- Service 启动去重: 1.5s 内防止重复 start
